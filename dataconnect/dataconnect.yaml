specVersion: "v1beta"
serviceId: "todo-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "todo-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
